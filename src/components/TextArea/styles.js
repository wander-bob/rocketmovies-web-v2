import styled from "styled-components";
import { theme } from "../../styles/theme";

export const Container = styled.textarea`
  width: 100%;
  border-radius: 1rem;
  padding: 1.6rem 1.9rem;
  min-height: 24rem;
  background-color: ${theme.gray._600};
  resize: none;

`